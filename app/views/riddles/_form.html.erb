<%= form_for @riddle do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :question %>
    <%= f.text_area :question, :class => "questionbox"%>

    <%= f.label :answer %>
    <%= f.text_field :answer %>

    <%= f.submit :class => "postbutton" %>
<% end %>
